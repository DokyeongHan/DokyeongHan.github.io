---
title: 다이나믹 프로그래밍
layout: post
date: '2019-05-04 23:24:00'
tags:
- algorithm
- dynamic programming
---

>*다이나믹 프로그래밍을 잊지 않기 위한 글이다.*

---

### 개념
DP = memoization + (재귀 or 반복)

중복된 계산을 막기 위해 계산된 결과를 배열에 저장하고,  

다음 계산에 필요할 때 저장된 값을 불러와서 중복을 없앤다.

---
### 문제 (백준 1463번 : 1로 만들기)

정수 X에 사용할 수 있는 연산은 다음과 같이 세 가지 이다.

1. X가 3으로 나누어 떨어지면, 3으로 나눈다.
2. X가 2로 나누어 떨어지면, 2로 나눈다.
3. 1을 뺀다.

정수 N이 주어졌을 때, 위와 같은 연산 세 개를 적절히 사용해서 1을 만들려고 한다. 연산을 사용하는 횟수의 최솟값을 출력하시오.

---
### 풀이<br>
for문에서 이전에 계산했던 값을 통해 다음 값을 이끌어낸다.

```python
i = int(input()) # 정수 N 입력
li = [] # 계산한 결과를 저장 할 리스트, li[3] 은 정수 3의 연산을 사용하는 횟수의 최솟값
li.append(0) # 정수 0의 연산을 사용하는 횟수의 최솟값 리스트에 추가
li.append(0) # 정수 1의 연산을 사용하는 횟수의 최솟값 리스트에 추가

for a in range(2,i+1): # 정수 2부터 입력한 정수 N까지 반복
    if(a%3 == 0): # 만약 3으로 나누어 떨어진다면
        li.append(min(li[a-1]+1,li[a//3] +1)) # N-1에서 1을 더한 경우와 N/3에서 3을 곱한 경우 중 작은 값 
    elif(a%2 == 0): # 만약 2로 나누어 떨어진다면
        li.append(min(li[a-1]+1,li[a//2] +1)) # N-1에서 1을 더한 경우와 N/2에서 2을 곱한 경우 중 작은 값
    else: # 2와 3으로 나누어 떨어지지 않는다면
        li.append(li[a-1] +1) # N-1에서 1을 더한 경우

print(li[i]) # 정수 N의 연산을 사용하는 횟수의 최솟값 출력
```